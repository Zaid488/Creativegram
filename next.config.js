/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["links.papareact.com", "google.com", "lh3.googleusercontent.com"],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
